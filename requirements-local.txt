# Optional local embedding demo; not required for coding checkpoints or pytest.
# Install with: python -m pip install -r requirements-local.txt
sentence-transformers==5.6.0
pyvi==0.1.1  # word segmentation required by the PhoBERT-based Vietnamese model
